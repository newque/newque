{
  "logLevel": "debug", // debug, info, notice, warning, error, fatal
  "listeners": [
    {
      "protocol": "HTTP",
      "name": "http9000",
      "host": "0.0.0.0",
      "port": 9000,
      "settings": {
        "backlog": 20 // Optional. Default 20. "backlog" for the listen(2) syscall
      }
    }
  ]
}